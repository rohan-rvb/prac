package org.example.prac.syntax.javabasics;

public class TypeCnversion {
    /*
    Order of conversion
    byte → short → int → long → float → double
    anything from left can be assigned to right one, but not reverse
    In arithmetic expression, the final data type of result is rightmost from above list, whichever is present.

     */
}
